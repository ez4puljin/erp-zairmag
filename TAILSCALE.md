# Өөрийн компьютер дээр ажиллуулах — Tailscale Serve

Системийг суурин компьютер дээрээ ажиллуулж, жолооч нарын утаснаас
интернэтээр хандах тохиргоо. **Public IP, домэйн худалдаж авах, роутерын
порт нээх шаардлагагүй.**

## Хэрхэн ажилладаг вэ

```
Жолоочийн утас ──WireGuard шифрлэлт──► Таны PC
  (Tailscale)                          ├── Tailscale Serve (HTTPS 443)
                                       ├── backend  :3000
                                       └── admin    :3001
```

Систем нээлттэй интернэтэд огт гарахгүй — зөвхөн `zairmag.erp@gmail.com`
бүртгэлийн tailnet-д нэвтэрсэн төхөөрөмж хандана.

## Хаяг

**`https://puljin.tailb3398e.ts.net`** — admin вэб болон гар утасны апп хоёул
энэ нэг хаягийг ашиглана.

| Зам | Хаашаа |
|---|---|
| `/` | admin (3001) |
| `/api/*` | backend (3000) |
| `/uploads/*` | барааны зураг (3000) |
| `/health` | эрүүл мэндийн шалгалт (3000) |

Бүгд нэг origin дор байгаа тул CORS тохируулах шаардлагагүй.

---

## Алхам 1: Үйлчилгээнүүдийг асаах

```bat
start.bat
```

backend (3000) ба admin (3001) хоёулаа асаалттай байх ёстой.

> **Анхаар:** `start.bat` нь admin-ыг хөгжүүлэлтийн горимд (`next dev`)
> ажиллуулдаг — энэ нь удаан бөгөөд байнгын хэрэглээнд зориулагдаагүй.
> Өдөр тутмын ашиглалтад production горим зөвлөмжтэй:
>
> ```bat
> cd admin && npm run build && npm run start -- -p 3001
> ```

## Алхам 2: Tailscale Serve тохируулах

```bat
tailscale-serve.bat
```

Энэ тохиргоо **компьютер унтрааж асаасан ч хадгалагдана** — нэг л удаа
ажиллуулна.

Шалгах:

```bat
curl https://puljin.tailb3398e.ts.net/health
```

`{"status":"ok","database":"ok",...}` ирвэл зөв.

## Алхам 3: Жолоочийн утас бэлтгэх

Хандах утас бүр **Tailscale-д бүртгэлтэй байх ёстой**:

1. Google Play-ээс **Tailscale** суулгах
2. `zairmag.erp@gmail.com` бүртгэлээр нэвтрэх (эсвэл admin console-оос
   урих холбоос илгээх)
3. **Always-on VPN** болгож тохируулах:
   Android Тохиргоо → Сүлжээ → VPN → Tailscale → Always-on VPN асаах

Always-on болгосноор жолооч Tailscale-ыг гар аргаар асаах шаардлагагүй —
утсаа асаамагц холбогдоно.

## Алхам 4: APK суулгах

APK файлыг утсанд хуулж суулгана. Тохиргоо шаардлагагүй — серверийн хаяг
апп дотор бичигдсэн.

> **Шинэчлэлт хийж байгаа бол:** апп сервер хаягийг төхөөрөмж дээрээ
> хадгалдаг. Хуучин хувилбар суусан утсан дээр шинэ хаяг автоматаар
> хэрэгжихгүй. Аппын "Server тохиргоо" хэсгээс гараар шинэчлэх, эсвэл
> хуучин аппыг устгаад шинээр суулгана.

---

## Асуудал шийдвэрлэх

### "Сервер холбогдохгүй байна"

| Шалгах зүйл | Хэрхэн |
|---|---|
| PC асаалттай юу | — |
| backend ажиллаж байна уу | `curl http://localhost:3000/health` |
| Serve тохиргоо байна уу | `tailscale serve status` |
| Утас tailnet-д холбогдсон уу | Tailscale апп нээж шалгах |

### Хаягаар хандахад 404 гарвал

Tailscale нь **Host гарчгаар** чиглүүлдэг. Tailnet-ийн IP хаягаар
(`100.78.89.97`) хандвал ажиллахгүй — заавал **нэрээр** нь хандана:
`puljin.tailb3398e.ts.net`.

### Tailscale бүртгэл солих юм бол

Хаяг доторх `tailb3398e` хэсэг нь tailnet бүрт өөр байдаг. Бүртгэл сольсон
тохиолдолд шинэ нэрийг олж:

```bat
tailscale status --json
```

Дараа нь `tailscale-serve.bat`, `mobile\eas.json`, `mobile\.env` доторх
хаягийг шинэчлээд APK-г дахин build хийнэ.

### Компьютер унтарвал

```bat
powercfg /change standby-timeout-ac 0
powercfg /change hibernate-timeout-ac 0
```

Windows update автоматаар restart хийхээс сэргийлж "Active hours"-ыг
тохируулна уу.

---

## Эрсдэл

- **Цахилгаан / интернэт тасрах = борлуулалт зогсоно.** UPS зайлшгүй.
- **Backup:** `backup.bat` (Google Drive) заавал автоматжуулна —
  SETUP.md-ийн Backup хэсгийг үзнэ үү. Гал, хулгайн эрсдэлд энэ л таны
  цорын ганц хамгаалалт.
- Хэрэв ирээдүйд Tailscale-гүйгээр (жишээ нь үйлчлүүлэгчид) хандах
  шаардлага гарвал `serve`-ийг `funnel` болгож солино — гэхдээ тэр үед
  систем нээлттэй интернэтэд гарна.
